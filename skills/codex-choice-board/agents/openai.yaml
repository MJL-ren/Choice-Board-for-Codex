interface:
  display_name: "Choice Board for Codex"
  short_description: "Ask with lightweight choices in Codex Desktop"
  default_prompt: "Create a lightweight choice board so I can answer several choices clearly."

policy:
  allow_implicit_invocation: true
