interface:
  display_name: "Choice Board for Codex"
  short_description: "Answer related questions in one lightweight board"
  default_prompt: "Use $codex-choice-board to ask me several related questions in one lightweight board."

policy:
  allow_implicit_invocation: true
