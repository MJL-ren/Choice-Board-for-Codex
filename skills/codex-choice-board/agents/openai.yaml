interface:
  display_name: "Choice Board for Codex"
  short_description: "Ask with lightweight choices in Codex Desktop"
  default_prompt: "Use $codex-choice-board to ask me for several choices in one lightweight board."

policy:
  allow_implicit_invocation: false
